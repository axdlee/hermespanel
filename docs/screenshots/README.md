# Screenshots

这个目录用于存放 HermesPanel 在 README 中展示的页面截图。

当前已经落地的文件：

- `dashboard-workbench.png`
- `config-workbench.png`
- `gateway-workbench.png`
- `extensions-workbench.png`

macOS 下可直接抓取桌面窗口：

```bash
npm run docs:capture:mac
```

如果执行失败，通常是因为当前终端还没有被授予：

- 屏幕与系统音频录制

建议在截图前先切到目标工作台，再执行脚本。
